spring:
  application:
    name: approval-workflow

  datasource:
#    url: ${DB_URL}
    url: jdbc:postgresql://localhost:5432/approval-workflow
#    username: ${DB_USERNAME}
    username: postgres
#    password: ${DB_PASSWORD}
    password: @Luweezy360#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
